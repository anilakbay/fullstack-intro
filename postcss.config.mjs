/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {}, // Autoprefixer'ı da eklemek iyi bir uygulamadır.
  },
};

export default config;
